# Hero media (pending)

The README references `docs/media/hero.gif` as the project's hero asset. That file
is **not captured yet** — it is a required user action (Layer A cannot record media).

To finish wiring it:

1. Record a short GIF/MP4 of a solve in action — ideally the terminal showing the
   agent writing `agent/fe.py` / `agent/model.py`, running leak-free CV, hitting the
   checkpoint gate, and/or the Streamlit dashboard (`kaggle-slayer-dashboard`)
   timeline view.
2. Save it as `docs/media/hero.gif` (or update the path in `README.md` if you use a
   different name/format).
3. The README image reference will then render with no further edits.

Until the file is dropped in, the README link will show a "pending" placeholder.
