You are picking up KaggleSlayer, an LLM-agent harness that drops a Gemini-driven Solver into a sandboxed workspace to autonomously solve tabular Kaggle competitions under a leak-free cross-validation contract (375 tests pass, ruff + mypy clean, real Gemini verified). It was just reset for its FIRST RELEASE. PLAN ONLY — verify with me before writing code. Read README.md, GOALS.md (or ROADMAP.md), CLAUDE.md, PORTFOLIO_STANDARD.md, and the design spec under docs/; orient in kaggle_slayer/harness/cv.py (the leak-free CV contract), harness/sandbox.py, harness/checkpoints.py, harness/journal.py + resume.py, agent/solver.py, agent/llm_client.py, the telemetry package, the dashboard, pyproject.toml, and ci.yml. Run `pytest -m "not slow"` to confirm the green baseline first. v1 scope: (1) a clean clone runs the agent loop end-to-end with NO API keys via a documented credential-free demo (fake-LLM / synthetic-comp path — a generator already exists at tests/fixtures/synthetic_comp.py; decide how to expose it given competitions/, *.csv, *.parquet are gitignored); (2) the real Gemini+Kaggle path has ≥1 fully worked reproducible example + an .env.example; (3) README leads with honest hero media and every claim matches what CI enforces; (4) airtight hygiene (no secrets/artifacts tracked; remove the half-finished competitions/titanic/); (5) docs tell one drift-free story; (6) promote to a real v1 with deferred work framed as roadmap; (7) fix the CI/docs mypy drift (CI runs mypy on harness only). OUT (roadmap only): live leaderboard/benchmark, MLflow artifact logging, CV↔LB backfill, dashboard diff/cross-comp pages, ADRs, .claude/ scaffolding, Phase 2/3. Do NOT regress the Hard Rules: leak-free CV contract, AST sandbox lint, checkpoint gate, append-only journal+resume, telemetry-never-crashes; keep the four-tier test structure. The .env has real Gemini + Kaggle creds — flag provider-side rotation to me. Bar = PORTFOLIO_STANDARD.md. Deliver a phased, sequenced plan (file-level changes, demo design, hero-media approach, doc rewrites, hygiene/version steps, per-phase verification). Present for approval before any code.
